# Local-only (gitignored)

- `gofer-v25-run.pem` — Lightsail SSH private key
- `lightsail-key.json` — AWS create-key-pair export

Generate on your machine via Lightsail/AWS CLI. Never commit or paste into chat.

Scripts default to `.tectonix/gofer-v25-run.pem` or `GOFER_LIGHTSAIL_KEY`.
